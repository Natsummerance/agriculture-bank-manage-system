# 项目结构重组说明

## 📋 重组概述

根据架构逻辑，对项目文件夹进行了合并和重组，避免不合理的冗余，使项目结构更加清晰整洁。

## ✅ 已完成的合并

### 1. 文档目录合并
**时间**: 2025-01-XX

**合并内容**:
- `md/` → `docs/md/`
- 所有项目文档统一放在 `docs/` 目录下
- PPT演示文稿保持在 `docs/presentations/`

**说明**: 
- 文档和资源统一管理
- 符合常见的项目结构规范（docs目录存放所有文档）

### 2. 测试目录合并
**时间**: 2025-01-XX

**合并内容**:
- `e2e/` → `tests/e2e/`
- `src/test/` → `tests/frontend/`
- 测试相关脚本 → `tests/scripts/`
- 后端测试文档和脚本 → `tests/backend/`

**说明**:
- 所有测试相关文件统一放在 `tests/` 目录
- 后端测试代码保留在 `backend/src/test/`（Maven标准结构）
- 测试脚本和文档整理到 `tests/` 目录下

### 3. 脚本目录整理
**时间**: 2025-01-XX

**整理内容**:
- 测试相关脚本移动到 `tests/scripts/`
- 服务管理脚本保留在 `scripts/`

**说明**:
- 测试脚本和测试代码放在一起，便于管理
- 服务启动等运维脚本独立管理

## 📁 新的项目结构

```
agriculture-bank-manage-system-master/
├── docs/                          # 文档和资源（已合并）
│   ├── md/                        # 项目文档
│   │   ├── architecture/         # 架构文档
│   │   ├── design/                # 设计文档
│   │   ├── features/              # 功能文档
│   │   ├── fixes/                 # 修复记录
│   │   ├── guides/                # 指南文档
│   │   ├── implementation/        # 实现文档
│   │   ├── reports/               # 报告文档
│   │   └── verification/           # 验证文档
│   └── presentations/             # PPT演示文稿
│
├── tests/                         # 测试目录（已合并）
│   ├── e2e/                       # E2E测试（Playwright）
│   ├── frontend/                  # 前端单元测试（Vitest）
│   ├── backend/                   # 后端测试文档和脚本
│   └── scripts/                   # 测试运行脚本
│
├── scripts/                       # 服务管理脚本
│   └── start-all-services.ps1
│
├── backend/                       # 后端服务
│   └── src/
│       ├── main/                  # 主代码
│       └── test/                  # 测试代码（Maven标准结构）
│
└── ...                            # 其他目录保持不变
```

## 🔄 路径变更对照表

| 旧路径 | 新路径 |
|--------|--------|
| `md/` | `docs/md/` |
| `e2e/` | `tests/e2e/` |
| `src/test/` | `tests/frontend/` |
| `scripts/run-tests.ps1` | `tests/scripts/run-tests.ps1` |
| `scripts/run-e2e-tests.ps1` | `tests/scripts/run-e2e-tests.ps1` |
| `scripts/test-connectivity.*` | `tests/scripts/test-connectivity.*` |

## 📝 更新的文件

以下文件已自动更新路径引用：
- README.md
- PROJECT_STRUCTURE.md
- 所有文档中的路径引用
- 测试配置文件
- 脚本文件

## 🎯 重组优势

1. **结构清晰**: 文档统一在docs，测试统一在tests
2. **易于维护**: 相关文件集中管理
3. **符合规范**: 遵循常见的项目结构规范
4. **减少冗余**: 避免目录分散和重复

## 🔗 相关文档

- [PROJECT_STRUCTURE.md](../../PROJECT_STRUCTURE.md) - 项目结构说明
- [README.md](../../README.md) - 项目总览

---

**重组完成时间**: 2025-01-XX  
**版本**: 2.0


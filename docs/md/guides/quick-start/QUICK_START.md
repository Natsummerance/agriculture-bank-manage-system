# 快速开始指南 - 功能性测试

## 📋 前置条件

1. **MySQL数据库** (端口 3306)
   - 数据库名: `agriverse`
   - 用户名: `root`
   - 密码: `Tdl@3731`
   - 确保MySQL服务已启动

2. **Java 21+** 和 **Maven 3.8+**
   - 用于运行后端服务

3. **Node.js 18+** 和 **npm**
   - 用于运行前端服务

## 🚀 快速启动

### 方法1: 使用启动脚本 (推荐)

```powershell
# 启动所有服务
.\scripts\start-all-services.ps1
```

这个脚本会:
- 检查MySQL服务状态
- 在新窗口中启动后端服务 (端口 8080)
- 在新窗口中启动前端服务 (端口 5173)

### 方法2: 手动启动

#### 1. 启动后端服务

```powershell
cd backend
mvn spring-boot:run
```

或者使用IDE:
- 打开 `backend` 项目
- 运行 `AgriverseAuthApplication.java`

#### 2. 启动前端服务

```powershell
# 在项目根目录
npm install  # 如果依赖未安装
npm run dev
```

## ✅ 验证服务运行

### 检查后端服务

```powershell
# 方法1: 使用测试脚本
.\scripts\test\test-system.ps1

# 方法2: 手动检查
Invoke-WebRequest -Uri "http://localhost:8080/api/auth/health"
```

预期响应:
```json
{
  "code": 0,
  "message": "操作成功",
  "success": true,
  "data": "OK"
}
```

### 检查前端服务

打开浏览器访问: http://localhost:5173

## 🧪 运行功能性测试

### 使用自动化测试脚本

```powershell
.\scripts\test\test-system.ps1
```

测试脚本会检查:
- ✅ 后端服务健康检查
- ✅ 发送验证码
- ✅ 用户注册
- ✅ 用户登录
- ✅ 获取用户信息
- ✅ 创建商品 (农户)
- ✅ 获取商品列表
- ✅ 买家商品列表

### 手动测试

#### 1. 认证功能测试

**健康检查:**
```powershell
Invoke-WebRequest -Uri "http://localhost:8080/api/auth/health"
```

**用户登录:**
```powershell
$body = @{
    phone = "13800138000"
    password = "password123"
    role = "farmer"
} | ConvertTo-Json

$response = Invoke-WebRequest -Uri "http://localhost:8080/api/auth/login" `
    -Method POST -Body $body -ContentType "application/json"
$response.Content
```

#### 2. 前端功能测试

1. 打开浏览器: http://localhost:5173
2. 测试登录流程
3. 测试注册流程
4. 测试各角色功能

详细测试步骤请参考 `test-guide.md`

## 📊 测试结果

测试脚本会输出测试结果汇总:

```
========================================
Test Results Summary
========================================

[PASS] Health Check
[PASS] Send Verification Code
[PASS] User Login
[PASS] Get User Info
[PASS] Create Product
[PASS] Get Product List

Total: 6 tests
Passed: 6
Failed: 0
```

## 🔧 常见问题

### 问题1: 后端启动失败

**错误**: `无法连接到数据库`

**解决方案**:
1. 检查MySQL服务是否运行
2. 检查 `backend/src/main/resources/application.yml` 中的数据库配置
3. 确保数据库 `agriverse` 已创建

### 问题2: 前端启动失败

**错误**: `端口 5173 已被占用`

**解决方案**:
```powershell
# 查找占用端口的进程
netstat -ano | findstr ":5173"

# 结束进程 (替换 PID 为实际进程ID)
taskkill /PID <PID> /F
```

### 问题3: Maven 未找到

**解决方案**:
1. 安装 Maven 并添加到 PATH
2. 或使用 IDE (IntelliJ IDEA / Eclipse) 运行后端

### 问题4: 测试脚本执行失败

**解决方案**:
```powershell
# 设置执行策略
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 然后运行脚本
.\scripts\test\test-system.ps1
```

## 📝 测试清单

- [ ] MySQL数据库运行正常
- [ ] 后端服务启动成功 (端口 8080)
- [ ] 前端服务启动成功 (端口 5173)
- [ ] 健康检查接口正常
- [ ] 用户登录功能正常
- [ ] 用户注册功能正常
- [ ] 商品管理功能正常
- [ ] 订单管理功能正常
- [ ] 前端页面加载正常
- [ ] 前端登录流程正常

## 📚 相关文档

- `test-guide.md` - 详细测试指南
- `scripts/test/test-system.ps1` - 自动化测试脚本
- `scripts/start-all-services.ps1` - 服务启动脚本
- `backend/README.md` - 后端API文档

## 🎯 下一步

1. 完成基础功能测试
2. 测试各角色特定功能
3. 测试业务流程完整性
4. 性能测试
5. 安全测试

---

**提示**: 如果遇到问题,请查看 `test-guide.md` 中的详细故障排查指南。


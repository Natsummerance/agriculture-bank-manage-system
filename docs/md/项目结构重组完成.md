# 项目结构重组完成

## 📋 重组概述

根据架构逻辑，对项目进行了彻底重组，使项目结构更加清晰整洁。

## ✅ 已完成的重组

### 1. 前端文件统一到frontend/
**时间**: 2025-01-XX

**移动的文件和目录**:
- `api/` → `frontend/api/`
- `apps/` → `frontend/apps/`
- `components/` → `frontend/components/`
- `config/` → `frontend/config/`
- `contexts/` → `frontend/contexts/`
- `hooks/` → `frontend/hooks/`
- `pages/` → `frontend/pages/`
- `roles/` → `frontend/roles/`
- `router/` → `frontend/router/`
- `stores/` → `frontend/stores/`
- `styles/` → `frontend/styles/`
- `utils/` → `frontend/utils/`
- `tests/frontend/` → `frontend/tests/`
- `App.tsx`, `main.tsx`, `index.html`, `index.css` → `frontend/`
- `package.json`, `vite.config.ts`, `tsconfig.json` 等配置文件 → `frontend/`

**说明**: 
- 所有前端代码统一放在 `frontend/` 目录
- 前端测试也移到 `frontend/tests/`
- 前端配置文件统一管理

### 2. 后端文件保持在backend/
**时间**: 2025-01-XX

**说明**: 
- 后端文件已经在 `backend/` 目录，保持不变
- 后端测试代码保留在 `backend/src/test/`（Maven标准结构）

### 3. 文档文件统一到docs/
**时间**: 2025-01-XX

**移动的文件**:
- `PROJECT_STRUCTURE.md` → `docs/PROJECT_STRUCTURE.md`
- 所有其他文档已在 `docs/md/` 目录

**说明**: 
- 所有文档统一放在 `docs/` 目录
- PPT演示文稿在 `docs/presentations/`
- 项目结构说明在 `docs/PROJECT_STRUCTURE.md`

### 4. 根目录清理
**时间**: 2025-01-XX

**结果**: 
- 根目录只保留 `README.md` 一个文档文件
- 所有其他文件都已移动到对应目录

## 📁 新的项目结构

```
agriculture-bank-manage-system-master/
├── frontend/                      # 前端项目（React + TypeScript）
│   ├── api/                       # API客户端
│   ├── apps/                      # 应用入口
│   ├── components/                # React组件
│   ├── roles/                     # 角色页面
│   ├── stores/                    # 状态管理
│   ├── tests/                     # 前端测试
│   └── ...                        # 其他前端文件
│
├── backend/                       # 后端项目（Spring Boot）
│   ├── src/
│   │   ├── main/                  # 主代码
│   │   └── test/                  # 测试代码
│   └── ...
│
├── docs/                          # 项目文档
│   ├── md/                        # Markdown文档
│   ├── presentations/             # PPT演示文稿
│   └── PROJECT_STRUCTURE.md       # 项目结构说明
│
├── tests/                         # 测试相关
│   ├── e2e/                       # E2E测试
│   ├── backend/                   # 后端测试文档
│   └── scripts/                   # 测试脚本
│
├── scripts/                       # 服务管理脚本
├── tools/                         # 工具脚本
└── README.md                      # 项目说明（根目录唯一文档）
```

## 🔄 路径变更对照表

| 旧路径 | 新路径 |
|--------|--------|
| `api/` | `frontend/api/` |
| `apps/` | `frontend/apps/` |
| `components/` | `frontend/components/` |
| `roles/` | `frontend/roles/` |
| `stores/` | `frontend/stores/` |
| `utils/` | `frontend/utils/` |
| `src/test/` | `frontend/tests/` |
| `PROJECT_STRUCTURE.md` | `docs/PROJECT_STRUCTURE.md` |

## 📝 更新的文件

以下文件已自动更新路径引用：
- README.md - 更新了所有文件路径引用
- docs/PROJECT_STRUCTURE.md - 更新了项目结构说明
- 所有文档中的路径引用

## 🎯 重组优势

1. **结构清晰**: 前端、后端、文档、测试完全分离
2. **易于维护**: 相关文件集中管理
3. **符合规范**: 遵循常见的项目结构规范
4. **根目录整洁**: 根目录只保留README.md，不堆砌文件

## 🔗 相关文档

- [README.md](../../README.md) - 项目总览
- [PROJECT_STRUCTURE.md](../PROJECT_STRUCTURE.md) - 项目结构说明
- [docs/md/README.md](README.md) - 文档索引

---

**重组完成时间**: 2025-01-XX  
**版本**: 3.0

